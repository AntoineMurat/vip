{{> vipsMenu}}

<fieldset class="adminOnTheRight">

	<legend>{{title}}</legend>

	<form action="#" method="POST">

		<label for="vip">VIP à supprimer</label>

		<select name="vip" id="vip">

			{{#each vips}}
				<option value="{{this.id}}">{{this.nom}} {{this.prenom}}</option>
			{{/each}}

		</select>

		<button type="submit" value="submit">Supprimer</button>

	</form>

</fieldset>